// Optional deployment configuration. Keep credentials out of tracked source.
// A deployment may replace this file in its packaged UI with its own restricted
// browser API key. Without one, location uses manual GPS-coordinate entry.
window.OSMapsConfig = { apiKey: "" };
